name: flutter_application_1
description: "Un projet Flutter pour la détection de texte et de dangers."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Packages existants
  camera: ^0.11.1
  google_mlkit_text_recognition: ^0.13.0
  google_mlkit_barcode_scanning: ^0.11.0 # Gardé au cas où
  path_provider: ^2.1.3
  cupertino_icons: ^1.0.8

  # --- AJOUTEZ CE PACKAGE ---
  # Pour faire des appels à l'API Gemini
  http: ^1.2.1 # Vérifiez la dernière version sur pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
